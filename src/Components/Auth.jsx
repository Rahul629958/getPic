import React from "react";
import ReactDOM from "react-dom";

export default function Auth()
{
    return (
        <React.StrictMode>
           <h1> This is Auth </h1>
        </React.StrictMode>
    );
}